<UserControl x:Class="BaseFramework.WpfHost.Views.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BaseFramework.Wpf.Controls;assembly=BaseFramework.Wpf">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="10. Schedule / Advanced Composite Example"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,8" />

        <Border Grid.Row="1"
                Margin="0,0,0,16"
                Padding="12"
                CornerRadius="8"
                Background="{StaticResource SurfaceBrush}">
            <TextBlock Text="This preserved example keeps the original calendar and richer composite object scenario alive while the rest of the host becomes a clearer learning gallery."
                       TextWrapping="Wrap"
                       Opacity="0.82" />
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" MinWidth="260" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="8"
                    Padding="8">
                <controls:ObjectInspectorControl x:Name="Inspector" />
            </Border>

            <GridSplitter Grid.Column="1"
                          Margin="2,0"
                          Background="{StaticResource SurfaceBrush}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ShowsPreview="True" />

            <controls:CalendarControl Grid.Column="2"
                                      x:Name="Schedule"
                                      EventsSource="{Binding ScheduledItems}"
                                      Margin="12,0,0,0" />
        </Grid>
    </Grid>
</UserControl>
