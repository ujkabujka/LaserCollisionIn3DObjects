<UserControl x:Class="BaseFramework.WpfHost.Views.InspectorExamplePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BaseFramework.Wpf.Controls;assembly=BaseFramework.Wpf">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,8" />

        <Border Grid.Row="1"
                Margin="0,0,0,16"
                Padding="12"
                CornerRadius="8"
                Background="{StaticResource SurfaceBrush}">
            <TextBlock x:Name="DescriptionText"
                       TextWrapping="Wrap"
                       Opacity="0.82" />
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8"
                Padding="8">
            <controls:ObjectInspectorControl x:Name="Inspector" />
        </Border>
    </Grid>
</UserControl>
