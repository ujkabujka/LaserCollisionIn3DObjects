<UserControl x:Class="LaserCollisionIn3DObjects.Wpf.Features.SourceCompletion.Views.SourceCompletionWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:helix="http://helix-toolkit.org/wpf"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.ColumnDefinitions><ColumnDefinition Width="420"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Source Completion" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock Text="Projected Source" FontWeight="SemiBold"/>
            <ComboBox ItemsSource="{Binding SourceCompletionWorkspace.AvailableProjectedSources}" SelectedItem="{Binding SourceCompletionWorkspace.SelectedProjectedSourceInput}" DisplayMemberPath="DisplayText" Margin="0,4,0,10"/>
            <TextBlock Text="Target Collision Scene" FontWeight="SemiBold"/>
            <ComboBox ItemsSource="{Binding SourceCompletionWorkspace.TargetCollisionScenes}" SelectedItem="{Binding SourceCompletionWorkspace.SelectedTargetCollisionScene}" DisplayMemberPath="Name" Margin="0,4,0,10"/>
            <WrapPanel><Button Content="Refresh Sources" Command="{Binding SourceCompletionWorkspace.RefreshSourcesCommand}" Margin="0,0,8,8"/><Button Content="Analyze Coverage" Command="{Binding SourceCompletionWorkspace.AnalyzeCoverageCommand}" Margin="0,0,8,8"/><Button Content="Generate Completed Source" Command="{Binding SourceCompletionWorkspace.GenerateCompletedSourceCommand}" Margin="0,0,8,8"/></WrapPanel>
            <TextBlock Text="{Binding SourceCompletionWorkspace.StatusMessage}" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SourceCompletionWorkspace.CompletionSummary}" TextWrapping="Wrap" Margin="0,4,0,0" FontWeight="SemiBold"/>
        </StackPanel>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="Source Completion Preview" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <helix:HelixViewport3D x:Name="SourceCompletionViewport" Grid.Row="1" ShowCoordinateSystem="True" ShowViewCube="True" ZoomExtentsWhenLoaded="True">
                <helix:SunLight/>
            </helix:HelixViewport3D>
            <StackPanel Grid.Row="2" Margin="0,8,0,0">
                <TextBlock Text="Original projected points: red/orange" Foreground="#555"/>
                <TextBlock Text="Completed synthetic points: green" Foreground="#555"/>
                <TextBlock Text="Preview shows source initial points only. Ray lines are not rendered here." Foreground="#555" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
