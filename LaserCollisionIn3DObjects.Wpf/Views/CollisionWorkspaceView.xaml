<UserControl x:Class="LaserCollisionIn3DObjects.Wpf.Views.CollisionWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:h="http://helix-toolkit.org/wpf"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="430" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Padding="12" BorderBrush="#D8D8D8" BorderThickness="0,0,1,0" Background="#FAFAFA">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Scene Editor" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" />

                    <!-- existing Scenes + Prisms unchanged -->
                    <GroupBox Header="Scenes" Margin="0,0,0,8" Padding="8">
                        <StackPanel>
                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding NewSceneName, UpdateSourceTrigger=PropertyChanged}"/></Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6"><Button Content="Create Scene" Command="{Binding CreateSceneCommand}" Width="110" Margin="0,0,8,0"/><Button Content="Delete Selected" Command="{Binding DeleteSelectedSceneCommand}" Width="120"/></StackPanel>
                            <ListBox ItemsSource="{Binding CollisionScenes}" SelectedItem="{Binding SelectedScene}" Height="120" DisplayMemberPath="Name"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Prisms" Margin="0,0,0,8" Padding="8">
                        <StackPanel>
                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding NewPrismName}"/></Grid>
                            <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="Pos X"/><TextBox Text="{Binding NewPrismPosX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Pos Y"/><TextBox Text="{Binding NewPrismPosY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Pos Z"/><TextBox Text="{Binding NewPrismPosZ}"/></StackPanel></UniformGrid>
                            <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="Rot X"/><TextBox Text="{Binding NewPrismRotX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Rot Y"/><TextBox Text="{Binding NewPrismRotY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Rot Z"/><TextBox Text="{Binding NewPrismRotZ}"/></StackPanel></UniformGrid>
                            <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="Size X"/><TextBox Text="{Binding NewPrismSizeX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Size Y"/><TextBox Text="{Binding NewPrismSizeY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Size Z"/><TextBox Text="{Binding NewPrismSizeZ}"/></StackPanel></UniformGrid>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Button Content="Add Prism" Command="{Binding AddPrismCommand}" Width="100" Margin="0,0,8,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <TextBlock Text="Array Radius:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBox Width="70" Text="{Binding NewPrismArrayRadius}"/>
                                </StackPanel>
                                <Button Content="Add Array" Command="{Binding AddPrismArrayCommand}" Width="95" Margin="0,0,8,0"/>
                                <Button Content="Remove Selected" Command="{Binding RemoveSelectedPrismCommand}" Width="120" Margin="0,0,8,0"/>
                                <Button Content="Delete All Prisms" Command="{Binding RemoveAllPrismsCommand}" Width="125"/>
                            </StackPanel>
                            <DataGrid ItemsSource="{Binding Prisms}" SelectedItem="{Binding SelectedPrism}" AutoGenerateColumns="False" CanUserAddRows="False" Height="130">
                                <DataGrid.Columns><DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/><DataGridTextColumn Header="Sx" Binding="{Binding SizeX}" Width="45"/><DataGridTextColumn Header="Sy" Binding="{Binding SizeY}" Width="45"/><DataGridTextColumn Header="Sz" Binding="{Binding SizeZ}" Width="45"/></DataGrid.Columns>
                            </DataGrid>
                            <GroupBox Header="Selected Prism" Margin="0,8,0,0" Padding="6">
                                <StackPanel>
                                    <TextBlock Text="No prism selected." Visibility="{Binding HasNoPrismSelection, Converter={StaticResource BoolToVisibility}}"/>
                                    <StackPanel Visibility="{Binding IsSelectedPrismEditable, Converter={StaticResource BoolToVisibility}}">
                                        <TextBlock Text="Position"/>
                                        <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="X"/><TextBox Text="{Binding SelectedPrismEditPositionX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Y"/><TextBox Text="{Binding SelectedPrismEditPositionY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Z"/><TextBox Text="{Binding SelectedPrismEditPositionZ}"/></StackPanel></UniformGrid>
                                        <TextBlock Text="Rotation"/>
                                        <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="X"/><TextBox Text="{Binding SelectedPrismEditRotationX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Y"/><TextBox Text="{Binding SelectedPrismEditRotationY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Z"/><TextBox Text="{Binding SelectedPrismEditRotationZ}"/></StackPanel></UniformGrid>
                                        <TextBlock Text="Size"/>
                                        <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="X"/><TextBox Text="{Binding SelectedPrismEditSizeX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Y"/><TextBox Text="{Binding SelectedPrismEditSizeY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Z"/><TextBox Text="{Binding SelectedPrismEditSizeZ}"/></StackPanel></UniformGrid>
                                        <Button Content="Apply Prism Changes" Command="{Binding ApplySelectedPrismChangesCommand}" Width="150"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Axisymmetric Light Sources" Margin="0,0,0,8" Padding="8">
                        <StackPanel>
                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding NewLightSourceName}"/></Grid>
                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Type" VerticalAlignment="Center"/><ComboBox Grid.Column="1" ItemsSource="{Binding LightSourceKinds}" SelectedItem="{Binding NewLightSourceKind}"/></Grid>

                            <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="Pos X"/><TextBox Text="{Binding NewLightSourcePosX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Pos Y"/><TextBox Text="{Binding NewLightSourcePosY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Pos Z"/><TextBox Text="{Binding NewLightSourcePosZ}"/></StackPanel></UniformGrid>
                            <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="Rot X"/><TextBox Text="{Binding NewLightSourceRotX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Rot Y"/><TextBox Text="{Binding NewLightSourceRotY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Rot Z"/><TextBox Text="{Binding NewLightSourceRotZ}"/></StackPanel></UniformGrid>
                            <UniformGrid Columns="2" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="RayCount"/><TextBox Text="{Binding NewLightSourceRayCount}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="TiltWeight"/><TextBox Text="{Binding NewLightSourceTiltWeight}"/></StackPanel></UniformGrid>
                            <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="Tilt Point X"/><TextBox Text="{Binding NewLightSourceTiltPointX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Tilt Point Y"/><TextBox Text="{Binding NewLightSourceTiltPointY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Tilt Point Z"/><TextBox Text="{Binding NewLightSourceTiltPointZ}"/></StackPanel></UniformGrid>

                            <GroupBox Header="Cylinder Parameters" Margin="0,0,0,6" Padding="6" Visibility="{Binding IsNewLightSourceCylinder, Converter={StaticResource BoolToVisibility}}">
                                <UniformGrid Columns="2"><StackPanel Margin="2"><TextBlock Text="Radius"/><TextBox Text="{Binding NewLightSourceRadius}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Height"/><TextBox Text="{Binding NewLightSourceHeight}"/></StackPanel></UniformGrid>
                            </GroupBox>

                            <GroupBox Header="Conical Frustum Parameters" Margin="0,0,0,6" Padding="6" Visibility="{Binding IsNewLightSourceConicalFrustum, Converter={StaticResource BoolToVisibility}}">
                                <UniformGrid Columns="3"><StackPanel Margin="2"><TextBlock Text="R1"/><TextBox Text="{Binding NewLightSourceRadiusStart}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="R2"/><TextBox Text="{Binding NewLightSourceRadiusEnd}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Length"/><TextBox Text="{Binding NewLightSourceLength}"/></StackPanel></UniformGrid>
                            </GroupBox>

                            <GroupBox Header="Circular Ogive Parameters" Margin="0,0,0,6" Padding="6" Visibility="{Binding IsNewLightSourceCircularOgive, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel>
                                    <UniformGrid Columns="4"><StackPanel Margin="2"><TextBlock Text="R1"/><TextBox Text="{Binding NewLightSourceRadiusStart}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="R2"/><TextBox Text="{Binding NewLightSourceRadiusEnd}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Length"/><TextBox Text="{Binding NewLightSourceLength}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="ArcRadius"/><TextBox Text="{Binding NewLightSourceArcRadius}"/></StackPanel></UniformGrid>
                                    <Grid Margin="0,4,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Ogive Curvature" VerticalAlignment="Center"/><ComboBox Grid.Column="1" ItemsSource="{Binding OgiveCurvatureDirections}" SelectedItem="{Binding NewLightSourceOgiveCurvatureDirection}"/></Grid>
                                </StackPanel>
                            </GroupBox>

                            <GroupBox Header="Hybrid Segments" Margin="0,0,0,6" Padding="6" Visibility="{Binding IsNewLightSourceHybrid, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <Button Content="Add Segment" Command="{Binding AddHybridSegmentCommand}" Width="110" Margin="0,0,8,0"/>
                                        <Button Content="Remove Selected Segment" Command="{Binding RemoveSelectedHybridSegmentCommand}" Width="170"/>
                                    </StackPanel>
                                    <ListBox ItemsSource="{Binding NewHybridSegments}" SelectedItem="{Binding SelectedNewHybridSegment}" DisplayMemberPath="Summary" Height="120" Margin="0,0,0,8"/>

                                    <GroupBox Header="Selected Segment" Padding="6">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Type" VerticalAlignment="Center"/><ComboBox Grid.Column="1" ItemsSource="{Binding HybridSegmentKinds}" SelectedItem="{Binding SelectedNewHybridSegment.SegmentKind}"/></Grid>
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Length" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding SelectedNewHybridSegment.Length}"/></Grid>

                                            <StackPanel Visibility="{Binding SelectedNewHybridSegment.IsCylinder, Converter={StaticResource BoolToVisibility}}">
                                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Radius" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding SelectedNewHybridSegment.Radius}"/></Grid>
                                            </StackPanel>

                                            <StackPanel Visibility="{Binding SelectedNewHybridSegment.IsConicalFrustum, Converter={StaticResource BoolToVisibility}}">
                                                <UniformGrid Columns="2" Margin="0,0,0,6">
                                                    <StackPanel Margin="2"><TextBlock Text="R1"/><TextBox Text="{Binding SelectedNewHybridSegment.RadiusStart}" IsEnabled="{Binding SelectedNewHybridSegment.IsRadiusStartEditable}"/></StackPanel>
                                                    <StackPanel Margin="2"><TextBlock Text="R2"/><TextBox Text="{Binding SelectedNewHybridSegment.RadiusEnd}"/></StackPanel>
                                                </UniformGrid>
                                            </StackPanel>

                                            <StackPanel Visibility="{Binding SelectedNewHybridSegment.IsCircularOgive, Converter={StaticResource BoolToVisibility}}">
                                                <UniformGrid Columns="3"><StackPanel Margin="2"><TextBlock Text="R1"/><TextBox Text="{Binding SelectedNewHybridSegment.RadiusStart}" IsEnabled="{Binding SelectedNewHybridSegment.IsRadiusStartEditable}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="R2"/><TextBox Text="{Binding SelectedNewHybridSegment.RadiusEnd}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="ArcRadius"/><TextBox Text="{Binding SelectedNewHybridSegment.ArcRadius}"/></StackPanel></UniformGrid>
                                                <Grid Margin="0,4,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Curvature" VerticalAlignment="Center"/><ComboBox Grid.Column="1" ItemsSource="{Binding OgiveCurvatureDirections}" SelectedItem="{Binding SelectedNewHybridSegment.OgiveCurvatureDirection}"/></Grid>
                                            </StackPanel>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>
                            </GroupBox>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Button Content="Add Source" Command="{Binding AddLightSourceCommand}" Width="95" Margin="0,0,8,0"/>
                                <Button Content="Remove Selected" Command="{Binding RemoveSelectedLightSourceCommand}" Width="130" Margin="0,0,8,0"/>
                                <Button Content="Regenerate Rays" Command="{Binding RegenerateLightSourceRaysCommand}" Width="120"/>
                            </StackPanel>
                            <DataGrid ItemsSource="{Binding LightSources}" SelectedItem="{Binding SelectedLightSource}" AutoGenerateColumns="False" CanUserAddRows="False" Height="130">
                                <DataGrid.Columns><DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/><DataGridTextColumn Header="Type" Binding="{Binding SourceKind}" Width="95"/><DataGridTextColumn Header="R/H" Binding="{Binding Radius}" Width="45"/><DataGridTextColumn Header="R1" Binding="{Binding RadiusStart}" Width="45"/><DataGridTextColumn Header="R2" Binding="{Binding RadiusEnd}" Width="45"/><DataGridTextColumn Header="#" Binding="{Binding RayCount}" Width="55"/></DataGrid.Columns>
                            </DataGrid>

                            <GroupBox Header="Projected Light Sources" Margin="0,8,0,0" Padding="6">
                                <StackPanel>
                                    <ListBox ItemsSource="{Binding ProjectedLightSources}"
                                            SelectedItem="{Binding SelectedProjectedLightSource}"
                                            Height="110">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" />
                                                    <TextBlock Text=" (" />
                                                    <TextBlock Text="{Binding Rays.Count}" />
                                                    <TextBlock Text=" rays)" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    <Button Content="Remove Selected Projected Light Source"
                                            Command="{Binding RemoveSelectedProjectedLightSourceCommand}"
                                            Margin="0,6,0,0"
                                            Width="230" />

                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="Selected Source" Margin="0,8,0,0" Padding="6">
                                <StackPanel>
                                    <TextBlock Text="No source selected." Visibility="{Binding HasNoSourceSelection, Converter={StaticResource BoolToVisibility}}"/>
                                    <StackPanel Visibility="{Binding IsSelectedSourceEditable, Converter={StaticResource BoolToVisibility}}">
                                        <TextBlock Text="{Binding SelectedObjectEditorType}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                        <TextBlock Text="Position"/>
                                        <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="X"/><TextBox Text="{Binding SelectedSourceEditPositionX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Y"/><TextBox Text="{Binding SelectedSourceEditPositionY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Z"/><TextBox Text="{Binding SelectedSourceEditPositionZ}"/></StackPanel></UniformGrid>
                                        <TextBlock Text="Rotation"/>
                                        <UniformGrid Columns="3" Margin="0,0,0,6"><StackPanel Margin="2"><TextBlock Text="X"/><TextBox Text="{Binding SelectedSourceEditRotationX}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Y"/><TextBox Text="{Binding SelectedSourceEditRotationY}"/></StackPanel><StackPanel Margin="2"><TextBlock Text="Z"/><TextBox Text="{Binding SelectedSourceEditRotationZ}"/></StackPanel></UniformGrid>
                                        <Button Content="Apply Source Changes" Command="{Binding ApplySelectedSourceChangesCommand}" Width="150"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Actions" Margin="0,0,0,8" Padding="8">
                        <StackPanel>
                            <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="Algorithm" VerticalAlignment="Center"/><ComboBox Grid.Column="1" ItemsSource="{Binding CollisionAlgorithms}" SelectedItem="{Binding SelectedCollisionAlgorithm}" /></Grid>
                            <StackPanel Orientation="Horizontal"><Button Content="Run Collision" Command="{Binding RunCollisionCommand}" Width="120" Height="30" Margin="0,0,8,0"/><Button Content="Export Hit Points CSV" Command="{Binding ExportHitPointsCsvCommand}" Width="160" Height="30" Margin="0,0,8,0"/><Button Content="Reset Demo Scene" Command="{Binding ResetDemoSceneCommand}" Width="140" Height="30"/></StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Results" Padding="8">
                        <StackPanel>
                            <DataGrid ItemsSource="{Binding HitResults}" AutoGenerateColumns="False" CanUserAddRows="False" Height="140">
                                <DataGrid.Columns><DataGridTextColumn Header="Ray" Binding="{Binding RayLabel}" Width="70"/><DataGridCheckBoxColumn Header="Hit" Binding="{Binding HasHit}" Width="45"/><DataGridTextColumn Header="Distance" Binding="{Binding Distance, StringFormat=F3}" Width="85"/><DataGridTextColumn Header="Prism" Binding="{Binding PrismName}" Width="*"/></DataGrid.Columns>
                            </DataGrid>
                            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="#444" Margin="0,6,0,0"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Column="1">
            <h:HelixViewport3D x:Name="Viewport" ShowCoordinateSystem="True" ShowViewCube="True" ZoomExtentsWhenLoaded="True">
                <h:HelixViewport3D.Camera>
                    <PerspectiveCamera Position="0,-30,30" LookDirection="0,30,-30" UpDirection="0,0,1" FieldOfView="45" />
                </h:HelixViewport3D.Camera>
                <h:SunLight />
                <h:GridLinesVisual3D Width="120" Length="120" MinorDistance="5" MajorDistance="10" />
            </h:HelixViewport3D>
        </Grid>
    </Grid>
</UserControl>
